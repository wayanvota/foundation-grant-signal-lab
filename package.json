{
  "name": "foundation-grant-signal-lab",
  "version": "2.0.0",
  "private": false,
  "description": "Stateless call design, deterministic eligibility testing, and filing-backed proposal diligence for foundation program officers.",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node api/server.js",
    "dev": "FRONTEND_ORIGIN=http://localhost:4173 node api/server.js",
    "migrate": "node api/migrate.js",
    "build:frontend": "node scripts/build-static.mjs",
    "check": "node --check api/server.js && node --check api/review.js && node --check api/callDesign.js && node --check src/provider.js && node --check src/ruleCompiler.js && node --check src/ruleSpec.js && node --check frontend/app.js && npm run check:forbidden",
    "check:forbidden": "node scripts/check-forbidden.mjs",
    "test": "node --test",
    "test:adversarial:live": "node scripts/run-adversarial-live.mjs",
    "serve:frontend": "npx serve frontend -l 4173"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mammoth": "^1.9.1",
    "multer": "^2.0.2",
    "pdf-parse": "^1.1.1",
    "zod": "^4.2.1"
  }
}
